[project]
name = "sentinel-virus-scanner"
version = "1.1.0"
description = "Classroom-safe hybrid Bloom-filter and SQLite virus scanner"
requires-python = ">=3.10"
dependencies = [
  "pefile>=2023.2.7",
]

[tool.pytest.ini_options]
pythonpath = ["."]
